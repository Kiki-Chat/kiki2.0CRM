# Demo-prep fix session — 2026-06-01

Running notes: root cause + fix + test + commit SHA per item. State as of session start: `cc841dd` (clean tree, origin/main current; then `310019f`/`2010258`/`9dcdeac` docs+absence shipped — HEAD `9dcdeac`).

**Operating rules in force:** prod = single shared Supabase (every write live); test ONLY against `+917879997839` / test org `c4dbf596-86fd-4484-88d9-095b2c082afb` (`kiki-test-007`). Commit per cluster, do NOT push, do NOT deploy without explicit per-cluster go. Additive migrations pre-authorized (flag each). Keep hermetic suite green + frontend build clean.

**Gates:** auto-proceed 1→2→3 (cosmetic, nothing needed). STOP for: Cluster 4 (B2 — present root cause first), Cluster 5 (needs metric definitions), Cluster 6 (needs CSVs).

---

# PHASE 0 — DIAGNOSIS (all items; no fixes yet)

## Cluster 1 — Admin settings = company only

### B7 — Remove "Personal Settings" from admin
**(a) Current behavior / root cause.** The "Persönliche Einstellungen" entry is in the sidebar profile dropdown, shown to **every** role. It opens `PersonalSettingsModal`, which holds: Profil (editable name, read-only email, read-only company), Darstellung (dark-mode toggle), Passwort ändern. There is no personal section *inside* the Company Settings page (`SettingsPage`) — the personal surface is purely this modal. So "remove personal settings from admin" = stop showing the modal entry to admins.
**(b) Files + lines.** Dropdown item: [Sidebar.tsx:181-186](frontend/src/components/layout/Sidebar.tsx#L181). Modal: [PersonalSettingsModal.tsx](frontend/src/components/PersonalSettingsModal.tsx) (name 99, email 104, company 112, dark-mode 122-127, change-pw 130-146). Dark-mode store: [theme.tsx](frontend/src/lib/theme.tsx) — **localStorage `hk-theme`, per-browser, NOT per-org**.
**(c) Proposed fix.** Gate the "Persönliche Einstellungen" `DropdownMenu.Item` on `!isAdmin` (employees keep it; admins don't). `useMe()` already exposes `isAdmin` in the sidebar. Keep the modal component for employees.
**(d) Open question / decision taken.** Dark-mode lives only inside that modal. Removing the modal for admins would silently drop dark-mode for admins (a regression). **Decision (flagged):** relocate the dark-mode toggle into Company Settings → **Design** section (appearance home; localStorage-backed so location is irrelevant functionally). If you'd rather just drop dark-mode for admins, say so and I'll remove the relocation.

### B8 — "Change Password" in Company Settings, last before Danger Zone
**(a) Current behavior.** Admin change-password only exists in the personal modal (being removed by B7). Company Settings (`SettingsPage`) has no password section.
**(b) Files + lines.** Nav model: [SettingsPage.tsx:103-124](frontend/src/pages/SettingsPage.tsx#L103) (`NAV_GROUPS` + `DANGER`). Section router: [SettingsPage.tsx:262-277](frontend/src/pages/SettingsPage.tsx#L262). Reusable change-pw endpoint: `POST /api/users/me/change-password` (already used by the modal, [PersonalSettingsModal.tsx:55-59](frontend/src/components/PersonalSettingsModal.tsx#L55)).
**(c) Proposed fix.** Add a new nav item `{ slug: 'passwort', label: 'Passwort', icon: Lock }` as the LAST section (own group "Konto" or appended) rendered immediately before the Gefahrenzone link; add a `PasswortSection` reusing the change-pw mutation (current/new/confirm, ≥8 chars, match check). No backend change.
**(d) Open question.** None.

### B1 — Admin shows its own "Meine Abwesenheit" — GO (employee surface exists)
**(a) Current behavior / root cause.** [nav.ts:40](frontend/src/components/layout/nav.ts#L40) lists `{ to: '/meine-abwesenheit', label: 'Meine Abwesenheit' }` with **no role flag**, so admins see it too. The page ([MyAbsencePage.tsx](frontend/src/pages/MyAbsencePage.tsx)) calls `GET/POST /api/employees/me/absences`, which resolves an **employee** from the caller — meaningless for an admin (= company, no employee record).
**Confirmed: the employee self-service surface EXISTS** (MyAbsencePage + route [App.tsx:48](frontend/src/App.tsx#L48) + `/api/employees/me/absences`). Admin-side absence management also exists separately in Mitarbeiter ([EmployeesPage.tsx](frontend/src/pages/EmployeesPage.tsx) — Abwesenheits-Kalender + Anträge/approval tabs). So removing "Meine Abwesenheit" from admin leaves admins their proper tool (manage all employees' absences) and leaves employees their self-service. **→ This is a GO, not a STOP.**
**(b) Files + lines.** Nav leaf [nav.ts:40](frontend/src/components/layout/nav.ts#L40); nav type [nav.ts:13-21](frontend/src/components/layout/nav.ts#L13) (only `adminOnly` exists = hides from employees; need the inverse); sidebar filter [Sidebar.tsx:69-73](frontend/src/components/layout/Sidebar.tsx#L69).
**(c) Proposed fix.** Add an `employeeOnly?: boolean` flag to `NavLeaf`, set it on the Meine-Abwesenheit leaf, and extend the `visibleNav` filter to drop `employeeOnly` leaves when `isAdmin`. (Cosmetic hide, consistent with the existing `adminOnly` pattern; route stays registered.)
**(d) Open question.** None.

## Cluster 2 — White-label identity (bottom badge + footer)

**Shared root cause.** The white-label chrome reads the wrong source. The bottom badge ([Sidebar.tsx:157-205](frontend/src/components/layout/Sidebar.tsx#L157)) shows `userName` = `session.user_metadata.full_name ?? 'HeyKiki User'` ([Sidebar.tsx:76](frontend/src/components/layout/Sidebar.tsx#L76)) and `session.user.email` — i.e. the **auth user**, not the company. The top header ([Sidebar.tsx:86-98](frontend/src/components/layout/Sidebar.tsx#L86)) hardcodes the `"HeyKiki"` wordmark. There is **no footer** in the app (`AppLayout` = Sidebar + Topbar + Outlet, [AppLayout.tsx](frontend/src/components/layout/AppLayout.tsx)). The white-label identity endpoint `/api/me` currently returns only `org_name` ([me.py:36-43](backend/app/api/routes/me.py#L36)) — not email/logo/address.

### B9 — company email → bottom badge · B10 — display name → company name · Logo → badge shows logo+name+email
**(b) Files + lines.** Badge [Sidebar.tsx:157-205](frontend/src/components/layout/Sidebar.tsx#L157); `userName` [Sidebar.tsx:76](frontend/src/components/layout/Sidebar.tsx#L76); `/api/me` [me.py](backend/app/api/routes/me.py); `useMe` shape [useMe.ts:7-14](frontend/src/lib/useMe.ts#L7). Org has `email` + `logo_url` + `address` ([SettingsPage Org interface 40-57](frontend/src/pages/SettingsPage.tsx#L40)).
**(c) Proposed fix.** **Backend (additive):** extend `/api/me` to also return `org_email`, `org_logo_url`, `org_address` (cache them under a new `org_identity` key; invalidate in `_update_org` [settings.py:159-168](backend/app/api/routes/settings.py#L159) and in logo upload/delete [settings.py:187-228](backend/app/api/routes/settings.py#L187)). **Frontend:** badge avatar = company `logo_url` (`<img>`) if set, else initials; name = company name; email = company email. Admin = company throughout; employees (a person) keep their own name/email in the badge.
**(d) Open question.** None (logo+name+email is fully specified).

### B10 (top header) + white-label wordmark
**(c) Proposed fix.** Replace the hardcoded `"HeyKiki"` top wordmark ([Sidebar.tsx:92](frontend/src/components/layout/Sidebar.tsx#L92)) and generic `"K"` box ([Sidebar.tsx:87-89](frontend/src/components/layout/Sidebar.tsx#L87)) with the company logo (if uploaded) + company name, so no "HeyKiki" shows in customer chrome. Fallback when no logo/name: company initial + name (or neutral "CRM Portal", never "HeyKiki").
**(d) Note.** This slightly exceeds the literal "bottom badge" wording but is required by the cluster theme ("no 'heykiki' anywhere customer-visible"). Flagging; trivially revertable if you want the wordmark kept.

### B11 — Footer = company contact, no heykiki
**(a) Current behavior.** No footer exists. (Heykiki support mailto lines exist in Abrechnung/KI-Nutzung tabs — those are billing-support, separate from B11.)
**(b) Files + lines.** Add in [AppLayout.tsx](frontend/src/components/layout/AppLayout.tsx) (below `<main>`), sourced from extended `/api/me`.
**(c) Proposed fix.** Slim footer bar: `{company name} · {street, PLZ Ort} · {company email as mailto}` styled `text-xs text-muted`, shown on all pages; hidden gracefully when org data missing. **DEFAULT taken:** company's own email from Company Data (per your instruction). kikichat.de forwarding alias = mail-routing infra → NOT implemented (flagged for you).
**(d) Open question.** None for the default. (Confirm only if you actually want the kikichat.de alias instead — that's infra, separate.)

## Cluster 3 — Google connection UX (B3)

**(a) Current behavior.** TWO sync surfaces: (1) the connect point — Settings → Kalender-Sync → Google card has a **"Jetzt synchronisieren" button** ([SettingsPage.tsx:974-978](frontend/src/pages/SettingsPage.tsx#L974)) plus the `syncNow` mutation ([SettingsPage.tsx:938-946](frontend/src/pages/SettingsPage.tsx#L938)); (2) the Calendar page's own "Sync" button ([CalendarPage.tsx:330-338] — KEEP). On connect today there is **NO auto-sync**: the OAuth popup callback persists the grant and returns, never syncing — events only appear after a manual click. The connection indicator (pill) already flips correctly on connect/disconnect ([SettingsPage.tsx:967](frontend/src/pages/SettingsPage.tsx#L967), driven by `GET /api/settings/oauth/connections`).
**(b) Files + lines.** Connect-point button to REMOVE: [SettingsPage.tsx:974-978](frontend/src/pages/SettingsPage.tsx#L974) (+ dead `syncNow` mutation 938-946, sync-status line 990-996 keep/trim). Backend callback to wire auto-sync: `backend/app/api/routes/oauth.py` callback (~142-210, right after `_persist_grant_and_link`); reuse path `link_purpose` (~213-250). Sync service: `backend/app/services/calendar_sync.py::pull_google_events(org_id)`. Manual sync endpoint (both buttons hit): `POST /api/calendar/sync` ([calendar_settings.py:41-66]).
**(c) Proposed fix.** Frontend: delete the connect-point Sync button + its now-unused mutation; keep indicator, disconnect, Calendar-page sync. Backend: in the OAuth callback, after `_persist_grant_and_link`, if `purpose=="calendar"` and `provider=="google"`, best-effort `pull_google_events(org_id)` (wrapped try/except — a sync failure must never fail the connect); mirror into `link_purpose` (reuse path). Test in `backend/tests/test_oauth_purpose.py`: assert calendar+google connect triggers exactly one `pull_google_events`, email connect does not.
**(d) Open question.** None blocking. Use `run_in_threadpool` (codebase has no `BackgroundTasks` usage) wrapped best-effort; `pull_google_events` is read-only + idempotent so it's safe to run in-callback. Wire reuse-path sync too (recommended).

## Cluster 4 — Employee recreate keeps old identity (B2) — DIAGNOSE, GATE ⛔

**(a) True root cause.** Soft-delete + reuse-login-by-email. Delete is a **soft-delete** ([employees.py:293-295](backend/app/api/routes/employees.py#L293) — `update({"deleted": True, "is_active": False})`), leaving `public.users` + Supabase `auth.users` intact. Recreate finds the surviving login by email and **reuses it without refreshing anything** ([employees.py:144-149](backend/app/api/routes/employees.py#L144) — `select("id")` then link; the name-writing `else` branch [161-169] is skipped). A fresh `employees` row is inserted with the NEW `display_name` (so the roster + password look right), but `users.full_name` keeps the OLD value — and that's what `/api/me`, `/api/users/me`, audit actor names, etc. render. The two name fields: `employees.display_name` (roster, correct) vs `users.full_name` (login identity, stale).
**(b) Files + lines.** Delete [employees.py:275-296](backend/app/api/routes/employees.py#L275); recreate reuse branch [employees.py:144-149](backend/app/api/routes/employees.py#L144); skipped name-write `else` [161-169]; fresh insert [196-207]; stale read surfaces [deps.py:46,57], [me.py:41](backend/app/api/routes/me.py#L41), [users.py:28], [kiki_zentrale.py:867-872]. Schema: `users.email` UNIQUE ([0001_init_schema.sql:41]); `employees.deleted` ([0008_employee_management.sql:11]); **no unique on `employees.email`** → soft-deleted dup rows accumulate.
**(c) Proposed fix (no migration).** In the reuse branch, refresh the reused login: `users.update({full_name, org_id, role})` + `auth.admin.update_user_by_id(user_id, {user_metadata:{full_name}})`. Never touch the password. Add a regression test asserting recreate-by-email with a new name yields the new `full_name` and correct role. (Longer-term option: `_delete` could also hard-delete/detach the login so recreate flows clean — higher risk, deferred.)
**(d) SECURITY-ADJACENT — YES (this is why we gate).** It is genuine login row-resurrection: **role/permissions resurrect** (reuse branch never rewrites `users.role`, so a deleted **admin** recreated as an **employee** silently keeps `org_admin`), the **old password/auth lineage** persists by design, and `org_id` is not re-asserted. The name leak is the visible tip. **Open question for you:** approve the (c) fix (which also rewrites role/org_id on reuse, closing the privilege-resurrection leg)? And should I additionally audit `kiki-test-007` for any recreated employee currently carrying `role='org_admin'` on its linked `users` row? **HOLDING for your review before implementing.**

## Cluster 5 — Dashboard metrics (B4) — DIAGNOSE now, FIX needs your input ⛔

**(a) Root cause (why Overview is right, the other tabs wrong).** Overview uses simple single-stage `count`/`eq` queries ([dashboard.py:97-147](backend/app/api/routes/dashboard.py#L97)) — trusted. The other tabs diverge for two structural reasons: **(A) fetch-window vs effective-date mismatch** — `_fetch_calls` filters by `created_at` ([dashboard.py:71-80]) but `_split_calls` buckets by `started_at or created_at` ([dashboard.py:83-93]); since `post_call.py:237-251` writes `status` hardcoded `"completed"` and `started_at` from (nullable) ElevenLabs metadata, calls land in the wrong month or fall back silently. **(B) dead/never-matching status filters** — `avg_dur`/`answered` filter `status=="completed"` (always true → no-op masking intent); any `"missed"` filter is always false (missed calls live in a separate `missed_calls` table, not the `calls` enum `open|in_progress|completed|deleted`).
- **KI-Nutzung tab** (`_ki_nutzung` [dashboard.py:290-364], front [KiNutzungTab.tsx]): `minutes_used`, `calls_count`, `avg_duration`, `estimated_days_remaining`, prev-month deltas, daily cumulative, top-callers, calls-by-hour — all keyed off the mismatched window; `minutes_used` ALSO disagrees with a *second* "minutes used" in `settings.py:_usage` [115-130] (sums by `created_at`, no split).
- **KI-Insights tab** (`_ai_insights` [dashboard.py:373-469], front [KiInsightsTab.tsx]): it's a rules engine, not a re-aggregation, so its counts are threshold-gated + suppressible subsets — "Überfällige Rechnungen" re-derives from `due_date` age across `sent|overdue` (≠ a literal `status=="overdue"` count), "Inaktive Kunden" uses `updated_at < now-182d` with **no soft-delete filter**, KVA-followup needs `sent_at` + age ≥ threshold.
**(b) Files + lines.** Backend [dashboard.py](backend/app/api/routes/dashboard.py): `_ki_nutzung` 290-364, `_ai_insights` 373-469, `_fetch_calls` 71-80, `_split_calls` 83-93, Overview 97-147; second minutes calc [settings.py:115-130](backend/app/api/routes/settings.py#L115); status/started_at source [post_call.py:237-251](backend/app/services/post_call.py#L237). Frontend `components/dashboard/KiNutzungTab.tsx`, `KiInsightsTab.tsx`, `lib/dashApi.ts`.
**(c) Proposed fix (candidate, pending your defs).** Standardize on ONE effective timestamp (`COALESCE(started_at, created_at)`) for BOTH fetch + bucket, applied in `settings.py:_usage` too; drop/replace the dead `status=="completed"` filter; align KI-Insights "overdue" with the intended definition; add the customer soft-delete filter + activity-based "inactive".
**(d) NEED FROM YOU.** Per-metric authoritative definition + one known-correct value for `kiki-test-007`. Key ambiguities: (1) canonical call timestamp = `started_at` (when call happened) or `created_at` (ingested)? + fallback when `started_at` null; (2) which "minutes used" is authoritative (Settings vs KI-Nutzung) and the current-month correct number; (3) what is an "answered/handled" call (all `completed`, or duration≥X, or excl. `missed_calls`); (4) should KI-Insights counts equal raw Overview/Finanzen counts or stay threshold-gated subsets; (5) "inactive customer" = not-edited vs no-business-activity. **HOLDING for definitions.**

## Cluster 6 — CSV import + missing-email (B5+B6) — needs your CSVs ⛔

### B5 — CSV mis-map
**(a) Root cause.** NOT a parser/delimiter/off-by-one bug — the backend parses correctly. `csv_import.parse_csv` uses Python `csv.DictReader` ([csv_import.py:45-51](backend/app/services/csv_import.py#L45)) which handles quoted commas + embedded newlines + multi-line records, and maps by header **name** via `_get` ([csv_import.py:55-65]). **The bug is the frontend auto-mapper:** [CsvImportModal.tsx:113-119](frontend/src/components/CsvImportModal.tsx#L113) does `hs.find(h => hints.some(kw => h.toLowerCase().includes(kw)))` — an **unanchored first-substring match across ~120 DATEV columns**. With `HINTS` ([CsvImportModal.tsx:40-58]) containing short substrings like `'str'` (matches many non-street columns) and `'name'` (matches `Vorname`/`Adressname`/…), the first wrong column wins → names bleed into address, phone into notes. Account/company name happens to map right because `Name` is the first "name" column.
**(b) Files + lines.** Frontend auto-map [CsvImportModal.tsx:40-58, 113-119](frontend/src/components/CsvImportModal.tsx#L113), mapping UI override [195-214]; backend (correct, for ref) [csv_import.py:38-65, 123-174], route [customers.py:186-200]; tests [backend/tests/test_csv_import.py].
**(c) Proposed fix.** Fix the auto-mapper: priority + exact/whole-word matching (exact header == hint before `includes`), remove dangerous short substrings (`'str'`, bare `'name'`), prevent one header binding two targets. Extract the mapper to a pure helper (`csvAutoMap.ts`) + unit-test it with the real DATEV header row (street→Strasse, notes→Bemerkung, phone→Telefon, name→Name). Keep the manual dropdown override.
**(d) NEED FROM YOU.** The **real DATEV CSV** (at least the header row + 1-2 sanitized rows) to pin the exact winning decoy columns + build the fixture. A sample with the ~120-col DATEV header was attached this session (Buderus/Adam… export); confirm it's representative. **HOLDING for your confirm/file.**

### B6 — Missing-email guard
**(a) Current behavior (partly works — verified).** Real send paths ALREADY guard: KVA send ([cost_estimates.py:347-363]) and invoice send ([invoices.py:340-355]) raise `400 "Keine Empfänger-E-Mail vorhanden…"`; employee create-with-login ([employees.py:141-143]) and resend-invite ([employees.py:327-329] → `"no_email"`) guard. Frontend send modals disable the send button on empty recipient ([CostEstimatesPage.tsx:321], [InvoicesPage.tsx:366]). **Gaps:** (1) no explicit *"no email connected"* warning in the two send modals (only a misleading SMTP hint [CostEstimatesPage.tsx:334]/[InvoicesPage.tsx:379]); (2) core `send_email()` ([email_send.py:70-92]) has no internal empty-recipient guard (callers must guard); (3) employee `"no_email"` sentinel → confirm the EmployeesPage toast copy is clear.
**(b) Files + lines.** As above.
**(c) Proposed fix.** Add a clear warning banner in both send modals when `customer_email` is empty (replacing the misleading hint); add a defensive empty-recipient guard at the top of `send_email()`; verify/clean the employee `no_email` toast. Add tests.
**(d) NEED FROM YOU.** Confirm in-scope send paths (KVA, invoice, employee reminder) and whether the sender line should also render empty. **HOLDING with Cluster 6.**

---

# PHASE 0 (round 2) — DIAGNOSIS for Clusters 7 & 8

## Cluster 7 — Employee invite email not firing at creation (+ folds in B2) — GATED ⛔
**(a) Invite-timing root cause.** Premise is partly wrong on `main`: create **does** call the invite send ([employees.py:180-194](backend/app/api/routes/employees.py#L180), `send_employee_welcome`). The real defects: (1) the create-path send is **silently best-effort** — wrapped in try/except that downgrades any failure to a soft `warning` string the UI shows as a normal success toast ([EmployeesPage.tsx flash(data.warning)]); and if `generate_set_password_link(new_user=True)` ([employees.py:158](backend/app/api/routes/employees.py#L158)) or the `users` insert throws, the outer except sets `user_id=None`/`link=None` ([:170-174](backend/app/api/routes/employees.py#L170)) so the send block at `:180` is **skipped entirely** (employee created, no login, no email). (2) The **recreate-by-email branch** ([:144-149](backend/app/api/routes/employees.py#L144)) reuses the surviving login and **sends nothing + resets nothing**. The only reliable sender is the **resend-invite endpoint** = the RefreshCw "Einladung erneut senden" button ([EmployeesPage.tsx:279-286,381-384](frontend/src/pages/EmployeesPage.tsx#L279), `POST /api/employees/{id}/resend-invite` [employees.py:363-377](backend/app/api/routes/employees.py#L363)) — an all-in-one try block that returns a real 502 on failure. That "sync"/resend is what reliably mails. (There is **no** calendar-style sync involved.)
**(c) `user_id`-inheritance audit (recreate reuses the same `user_id` because delete is soft + `users.email` is globally unique).**
- **Google/calendar OAuth grant (`oauth_connections`), `oauth_purpose_links`, `email_configs` → ORG-scoped (`org_id`), NOT inherited.** ✅ A recreated employee does NOT inherit the deleted person's Google connection (there is no per-user grant).
- **`inquiries.assigned_to`, `appointments.assigned_to`, `*.created_by`, audit `actor_id` → keyed by `users.id` → WOULD INHERIT** (attribution/ownership + audit integrity).
- **`assigned_employee_id`, `time_entries.employee_id`, `employee_absences.employee_id` → keyed by `employees.id` → orphaned to the dead row, NOT inherited.**
- **Supabase `auth` sessions + MFA factors → survive on `auth.users` → WOULD INHERIT (highest severity)** — the prior holder can still be logged in / hold 2FA on the reused login.
**(d) Proposed COMBINED fix (one approval covers invite-timing + full B2):**
1. **Create** → keep the immediate send but make "no login created" visibly distinct from "login created, email pending" (don't bury a hard failure in a success toast).
2. **Recreate-by-email** → true re-provision: `users.update({full_name, role, org_id})` + mint a **recovery** link + `send_employee_welcome` (fresh invite) — so the old password stops being the way in and the new hire gets mailed. Never silently retain the old password.
3. **Neutralize inherited access on recreate** → `auth.admin.sign_out(user_id)` + delete MFA factors before handing over (revokes the prior holder's sessions/2FA). **Open Q:** confirm the pinned `supabase` client exposes these (else use the Admin REST API).
4. **Recommended (cleanest):** hard-delete the login on employee delete (`auth.admin.delete_user` — pattern already in `provisioning.py:149` — + delete the `public.users` row) so recreate takes the clean new-user path and the whole inheritance class disappears. Keep the `employees` row soft-deleted for history.
**Migration:** none required. *Optional additive:* partial unique `employees(org_id, lower(email)) where deleted=false`. **Open product Qs for Amber:** hard-delete login on delete (4) vs soft+reprovision (2+3)? scrub `created_by`/audit `actor_id` on departure or retain as history? reuse the soft-deleted `employees` row vs insert a new one? **HOLDING — present with B2, one approval.**

## Cluster 8 — Stale email pre-filling a search field — CLEAN FRONTEND, auto-fix ✅
**(a) Root cause = (c) BROWSER AUTOFILL.** Ruled out shared React state (no `createContext`/module-level var ties search to email; every field is isolated `useState`) and stale store (no `defaultValue`/store binding). The employee email `<input>` ([EmployeesPage.tsx:712](frontend/src/pages/EmployeesPage.tsx#L712) + edit at :826) has **no `type`/`name`/`id`/`autoComplete`**, so Chrome classifies it (placeholder `email@example.de` + "E-Mail" label) as an email field and remembers the value; the **attribute-less search inputs** ([Topbar.tsx:33-36](frontend/src/components/layout/Topbar.tsx#L33), [EmployeesPage.tsx:299-304](frontend/src/pages/EmployeesPage.tsx#L299), [CustomersPage.tsx:106-111](frontend/src/pages/CustomersPage.tsx#L106), CatalogPage/CallLogsPage/CustomerDetailPage/projectTabs) then receive that remembered email via autofill.
**(b/c) Fix (attributes only, no state change):** add `type="search" name="<unique>" autoComplete="off"` to the search inputs; add `type="email" name="employee_email" autoComplete="off"` to the employee email inputs. Severs the autofill link.
**(d) VERDICT: CLEAN, ISOLATED, PURE-FRONTEND — does NOT share root with B2.** → auto-fixing per instruction.

# FIX LOG (appended as each cluster ships)

## Cluster 1 — Admin settings = company only ✅ (commit `c644a4b`)
- **B1** — Added `employeeOnly?: boolean` to `NavLeaf` ([nav.ts:18-25](frontend/src/components/layout/nav.ts#L18)); set `employeeOnly: true` on the Meine-Abwesenheit leaf ([nav.ts:45](frontend/src/components/layout/nav.ts#L45)); extended the sidebar filter with a `hideLeaf` helper so `employeeOnly` leaves drop for admins (and `adminOnly` still drop for employees) ([Sidebar.tsx:68-76](frontend/src/components/layout/Sidebar.tsx#L68)). Admins now manage absences only via Mitarbeiter; the employee self-service page is untouched.
- **B7** — Gated the "Persönliche Einstellungen" dropdown item on `!isAdmin` ([Sidebar.tsx:181-192](frontend/src/components/layout/Sidebar.tsx#L181)); employees keep it, admins don't. Dark-mode (the only non-identity control in that modal) **relocated** to Company Settings → Design → "Darstellung / Dunkles Design" ([SettingsPage.tsx DesignSection](frontend/src/pages/SettingsPage.tsx)) so admins don't lose it (localStorage-backed, applies live). **Flagged decision** — say the word to drop dark-mode for admins instead.
- **B8** — New "Konto → Passwort" nav group rendered LAST, immediately before Gefahrenzone ([SettingsPage.tsx NAV_GROUPS](frontend/src/pages/SettingsPage.tsx)); `PasswortSection` reuses `POST /api/users/me/change-password` (current/new/confirm, ≥8 chars, match check). No backend change.
- **Test:** `npm run build` (tsc -b + vite) clean; preview boots with **0 console errors**. Live admin render proof (sidebar without Meine-Abwesenheit + Persönliche Einstellungen; Settings Passwort section) **pending Amber's login** — password held by her, I won't type it.
- **Files:** `frontend/src/components/layout/nav.ts`, `frontend/src/components/layout/Sidebar.tsx`, `frontend/src/pages/SettingsPage.tsx`.

## Cluster 2 — White-label identity (bottom badge + footer) ✅ (commit `978f523`)
- **Backend (additive, no migration):** `/api/me` now returns `org_email` / `org_logo_url` / `org_address` alongside `org_name`, loaded via a new `_org_identity` cached under key `org_identity` ([me.py](backend/app/api/routes/me.py)). Cache invalidated in `_update_org` (key switched `org_name`→`org_identity`) and added to logo upload + delete ([settings.py:159-228](backend/app/api/routes/settings.py#L159)). Confirmed `org_name` cache key had only these two consumers, so the switch is safe.
- **B9/B10/Logo (bottom badge):** badge now shows the COMPANY for the admin login — `badgeName`/`badgeEmail`/`badgeLogo` = company name + contact email + logo (falls back to initials when no logo); employees (a person) keep their own name/email ([Sidebar.tsx:62-82, 160-205](frontend/src/components/layout/Sidebar.tsx#L62)). `useMe` `Me` type extended ([useMe.ts:7-18](frontend/src/lib/useMe.ts#L7)).
- **B10 (top header):** replaced the hardcoded "HeyKiki" wordmark + "K" box with the company logo (if uploaded) + company name; neutral `CRM Portal` subtitle, `Building2` fallback icon — no HeyKiki in customer chrome. **Flagged** — exceeds the literal "bottom badge" wording; trivially revertable.
- **B11 (footer):** new white-label footer in `AppLayout` — company name · address · contact email (mailto), sourced from `/api/me` (works for employees too), hidden when org data missing ([AppLayout.tsx](frontend/src/components/layout/AppLayout.tsx)). **DEFAULT taken:** company's OWN email (kikichat.de forwarding alias = infra, NOT implemented — flagged).
- **Test:** `npm run build` clean; `test_me_endpoint.py` strengthened to assert the new identity fields (+ null-org case); full hermetic suite **328 passed** (6 live deselected); backend restarted fresh + live-checked (`/health` 200, `/api/me` 401 unauth = route live, in openapi). Authenticated badge/footer render proof **pending Amber's login**.
- **Files:** `backend/app/api/routes/me.py`, `backend/app/api/routes/settings.py`, `backend/tests/test_me_endpoint.py`, `frontend/src/lib/useMe.ts`, `frontend/src/components/layout/Sidebar.tsx`, `frontend/src/components/layout/AppLayout.tsx`.

## Cluster 3 — Google connection UX (B3) ✅ (commit `db589ab`)
- **Backend (auto-sync on connect):** new best-effort `_auto_sync_calendar(org_id, provider, purpose)` ([oauth.py](backend/app/api/routes/oauth.py)) calls `calendar_sync.pull_google_events(org_id)` only for `purpose=="calendar"` + `provider=="google"`. Wired into the OAuth callback (after `_persist_grant_and_link`, via `run_in_threadpool`) AND the reuse-link path (`link_purpose._do`). Wrapped try/except → a sync failure never fails the connect (grant already persisted; pull is read-only + idempotent).
- **Frontend:** removed the "Jetzt synchronisieren" button + its now-dead `syncNow` mutation from the Kalender-Sync connection card ([SettingsPage.tsx CalendarProviderCard](frontend/src/pages/SettingsPage.tsx)); kept the connection pill, the account line, Trennen, and the last-synced status line (empty-state text updated to "Wird beim Verbinden automatisch synchronisiert … Manuelle Synchronisierung auf der Kalender-Seite."). The **Calendar page's** sync button is untouched.
- **Test:** 3 new tests in `test_oauth_purpose.py` (`_auto_sync_calendar` pulls for google+calendar · skips email + non-google · is best-effort on raise); updated the existing reuse-link test to mock `pull_google_events` + assert one call. `npm run build` clean; full hermetic suite **331 passed**; backend restarted fresh + live-checked (health 200, callback + `/api/calendar/sync` routes live, no boot errors). Authenticated render proof (no connect-point sync button) **pending Amber's login**.
- **Files:** `backend/app/api/routes/oauth.py`, `backend/tests/test_oauth_purpose.py`, `frontend/src/pages/SettingsPage.tsx`.
- **Note:** auto-sync runs in-callback via `run_in_threadpool` (codebase has no `BackgroundTasks` usage). Adds a little latency to the popup close on first connect; acceptable + consistent with the callback's existing threadpool calls.

## Cluster 8 — Stale email in search field (browser autofill) ✅ (commit `d40aa38`)
- **Root cause:** browser heuristic autofill — attribute-less `<input>`s. Chrome learned the employee-email value (no `type`/`name`/`autoComplete`) and offered it into the equally attribute-less search boxes.
- **Fix (attributes only, no state change):** the 2 employee email inputs → `type="email" name="employee_email" autoComplete="off"` ([EmployeesPage.tsx:715,829](frontend/src/pages/EmployeesPage.tsx#L715)); all 7 search inputs → `type="search" name="<unique>" autoComplete="off"` (Topbar global, Employees, Customers, Catalog, CallLogs anfragen, CustomerDetail anfragen, projectTabs add-employee). Distinct `name`s so the browser never shares values between them.
- **Test:** `npm run build` clean; grep confirms **no** search input lacks the guard + both email inputs guarded. **Render-proof note:** the autofill bug only reproduces in a browser that has *stored* the email (Amber's), so the MCP preview (fresh browser, empty autofill store) can neither reproduce nor disprove it — the fix is the canonical, deterministic autofill suppression; behavioral confirm belongs in Amber's browser. Verdict from diagnosis: clean isolated frontend, **no B2 overlap**.
- **Files:** `frontend/src/components/layout/Topbar.tsx`, `frontend/src/pages/EmployeesPage.tsx`, `frontend/src/pages/CustomersPage.tsx`, `frontend/src/pages/CatalogPage.tsx`, `frontend/src/pages/CallLogsPage.tsx`, `frontend/src/pages/CustomerDetailPage.tsx`, `frontend/src/pages/projectTabs.tsx`.

## Cluster 6 — CSV import (B5) + missing-email guard (B6) ✅ (commit `13a158f`)
- **B5 root cause (confirmed against Amber's real CSV):** NOT a parser bug — the backend `csv.DictReader` parses the wide DATEV export (120 cols, quoted commas, multi-line `Bemerkung`) correctly and maps by header NAME. The bug is the **frontend auto-mapper** ([CsvImportModal.tsx onPick](frontend/src/components/CsvImportModal.tsx)): unanchored first-substring matching mis-bound **phone→"Titel"** (`'tel'`⊂Titel), **city→"Suchwort"** (`'ort'`⊂Suchwort), **full_name→"Kurzname"** (`'name'`⊂Kurzname).
- **B5 fix:** two-pass mapper — Pass 1 EXACT header==hint (priority order), Pass 2 substring fallback but only for hints ≥4 chars, no header used twice. Verified by tracing the **real** DATEV header → `{full_name:"Titel+Vorname+Name", email:"Mail", phone:"Telefon", phone2:"Mobil", street:"Strasse", postal_code:"PLZ", city:"Ort", notes:"Bemerkung", customer_number:"Kundennummer"}` (all correct). Manual dropdown override still available.
- **B5 test:** backend `test_import_customers_real_datev_mapping` ([test_csv_import.py](backend/tests/test_csv_import.py)) — real DATEV header w/ decoy columns + a multi-line quoted Bemerkung + the corrected mapping → asserts name/phone/Ort/notes/number land right. (No frontend test runner exists; the mapper logic was verified via a throwaway node trace against the real file.)
- **B6 (verified partly-works):** real send paths already guard (KVA/invoice send → 400). **Added:** a clear "⚠ keine E-Mail hinterlegt" warning in the KVA + invoice send modals when the customer has no email (recipient already empty + send already disabled) ([CostEstimatesPage.tsx], [InvoicesPage.tsx]); a fail-safe guard at the top of `send_email()` that rejects an empty recipient before any tier/DB lookup ([email_send.py:88-92](backend/app/services/email_send.py#L88)). Employee reminder path already guards (`no_email` sentinel + 400 on login-create).
- **Test:** `npm run build` clean; full hermetic suite **335 passed** (+1 CSV DATEV, +3 email-guard params); backend restarted fresh (no boot errors). Authenticated render proof (send-modal warning) **pending Amber's login**.
- **Files:** `frontend/src/components/CsvImportModal.tsx`, `backend/tests/test_csv_import.py`, `frontend/src/pages/CostEstimatesPage.tsx`, `frontend/src/pages/InvoicesPage.tsx`, `backend/app/services/email_send.py`, `backend/tests/test_email_send_fallback.py`.

## Cluster 5 — Dashboard KI-Nutzung filter (B4) ✅ (commit `647259c`)
- **Amber's direction:** "monthly call mins … with the additional filters … specific range, day, weekly" → added a **Tag / Woche / Monat / Zeitraum** selector to the KI-Nutzung tab.
- **Backend:** `/api/dashboard/ki-nutzung` now takes `period` (day|week|month|range) + `from_date`/`to_date`. New `_ki_window` computes the Berlin-local window + an equal-length previous window (for the delta). The usage metrics (minutes/calls/avg/series/top-callers/by-hour) scope to the window; the **monthly contingent** (quota bar, `estimated_days_remaining`, `over_quota`) stays monthly always (`month_minutes_used`). Calls bucket by `COALESCE(started_at, created_at)` consistently. Dropped the dead `status=='completed'` avg-duration filter (every persisted call is 'completed', so it was a no-op masking intent). Series = hourly for a single day, else per-day. ([dashboard.py:289-410](backend/app/api/routes/dashboard.py#L289)).
- **Frontend:** period selector (segmented + a `Zeitraum` custom date range that applies once both dates are set); KPI/chart/panel labels reflect the selected period; trend badges compare to the previous window; quota bar + Kontingent reference line only render in Monat view. Type `KiNutzungData` updated (`series`/`period`/`series_x_label`/`month_minutes_used`/`previous_*`). ([KiNutzungTab.tsx](frontend/src/components/dashboard/KiNutzungTab.tsx), [dashApi.ts](frontend/src/lib/dashApi.ts)).
- **KI-Insights note:** Amber's input addressed only the "Use" tab. KI-Insights is a threshold-gated rules engine (correct-by-design), left as-is pending any specific definition — flag if you still see wrong numbers there.
- **Test:** new `backend/tests/test_dashboard_ki_nutzung.py` (6) — `_ki_window` day/week/month/range boundaries + `_ki_nutzung` scopes a recent call to the month + excludes out-of-window. `npm run build` clean; full hermetic suite **341 passed**; backend restarted + live-checked (endpoint exposes `period`/`from_date`/`to_date`, 401 unauth). Authenticated render proof (selector + filtered numbers + a known-correct value) **pending Amber's login**.
- **Files:** `backend/app/api/routes/dashboard.py`, `backend/tests/test_dashboard_ki_nutzung.py`, `frontend/src/lib/dashApi.ts`, `frontend/src/components/dashboard/KiNutzungTab.tsx`.

## Cluster 7 + B2 — invite-on-create + recreate re-provision ✅ (commit `2836564`, APPROVED: reprovision/no auth-deletion)
- **Implemented (reuse/recreate branch of `_create`, [employees.py:147-187](backend/app/api/routes/employees.py#L147)):** on recreate-by-email the surviving login is now fully **re-provisioned** — (a) `users.update({full_name, role, org_id})` + `auth.admin.update_user_by_id` metadata (fixes the stuck name AND **demotes** a resurrected admin→employee role); (b) **revoke the prior holder's sessions** via the GoTrue admin logout REST endpoint (new best-effort `employee_invite.revoke_user_sessions` — the high-level `auth.admin.sign_out` needs a JWT, not a user id); (c) reset the credential + send a **fresh invite** (recovery link, never a password). Soft-delete unchanged; **no auth account deleted** (per your approval).
- **Inheritance outcome:** Google/calendar grant is **org-scoped → never inherited** (confirmed). Sessions/MFA: sessions revoked on recreate (MFA factor deletion not exposed by the pinned client; sessions are the access vector and are revoked). Attribution FKs (`created_by`, audit `actor_id`) retained as historical authorship (your default); `assigned_to` work-reassignment inheritance is a known low-priority remainder (not in approved scope).
- **Create path:** already sends the invite on create (it was the *recreate* path that silently sent nothing) — left intact; the failure warning is surfaced to the admin.
- **Decision note:** kept inserting a FRESH `employees` row on recreate (clean HR slate) rather than reactivating the soft-deleted one — avoids resurrecting the deleted person's absences/balances, consistent with the anti-resurrection goal. (Reverses my earlier "reactivate" lean; flagging.)
- **Test:** 3 new in `test_wave2_employee_tiers.py` — recreate re-provisions identity (name refreshed + **role demoted** + org reset + sessions revoked + fresh recovery invite, no password) · admin-logout helper hits `/auth/v1/admin/users/{id}/logout` with the service key · raises on non-2xx. Full hermetic suite **344 passed**; backend restarted clean.
- **YOUR VERIFY (test org, needs admin login + a test inbox — I can't auth/read mail):** create → invite received; recreate same email/new name → old password fails, new invite works, name+role correct on /api/me + sidebar; no inherited Google grant. The DB/identity side is locked by the hermetic tests; the email-delivery + old-password-fails checks are live-only.
- **Migration:** none (optional additive partial-unique on active `(org_id, email)` not applied — flag if you want it).
- **Files:** `backend/app/services/employee_invite.py`, `backend/app/api/routes/employees.py`, `backend/tests/test_wave2_employee_tiers.py`.

## Round 3 (Amber field-test feedback) — email footer + dashboard data

### Email-template footer white-label ✅ (commit `458aecf`)
- **Issue:** the in-app footer was fixed (Cluster 2), but the **email** shell still hardcoded "Heykiki" / "Kiki-Chat GmbH" / info@kikichat.de (seen in a sent invoice).
- **Fix:** `email_templates._SHELL` footer → `@@FOOTER@@`, built per-send from the SENDING company's own name + address + contact email (threaded `org.email` + `addr_line(org.address)` from the KVA + invoice senders); neutral "Ihr Dienstleister" fallback (never HeyKiki). Welcome email shows company name + disclaimer.
- **Test:** updated `test_email_templates` (no Heykiki/Kiki-Chat/kikichat.de; asserts company email + address present). Suite 345→ green.
- **Files:** `email_templates.py`, `cost_estimates.py`, `invoices.py`, `tests/test_email_templates.py`.

### Dashboard "wrong data" — diagnosed (NOT a regression) + rolling-window fix ✅ (commit `84991e9`)
- **Root cause (DB-verified):** all **19 test calls are dated May 24–30**; it's June 2, so the calendar-month default ("dieser Monat") was genuinely empty → "0 / -19 vs previous month". `_anrufe` (the Anrufe tab) is **code I never touched this session** and used the same calendar-month logic, which is why BOTH tabs emptied at month rollover. Same code, new month — not a commit regression.
- **Fix:** switched the period windows to **ROLLING** (`_period_window`): Tag = today, Woche = last 7 days, Monat = last 30 days; applied to BOTH `_anrufe` + `_ki_nutzung`. Added the **same Tag/Woche/Monat/Zeitraum filter to Anrufe** (shared `usePeriodFilter` hook). The monthly AI-minute contingent (quota bar + Restlaufzeit) stays calendar-month, decoupled from the analytics window. Trend label "ggü. Vormonat" → "Vorperiode".
- **Live-verified on the real test org** (read-only call into the handlers): Monat → **19 calls / 30 min** (was 0), Woche → 16 calls / 25 min, Tag → 0 (no calls today). `month_minutes_used` (June quota) = 0, correct.
- **Test:** `test_dashboard_ki_nutzung` updated for rolling boundaries + `_anrufe` period test (7 pass). Full hermetic suite **346 passed**; build clean; backend restarted.
- **Files:** `dashboard.py`, `tests/test_dashboard_ki_nutzung.py`, `dashApi.ts`, `dashboard/shared.tsx`, `dashboard/AnrufeTab.tsx`, `dashboard/KiNutzungTab.tsx`.
