VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=http://localhost:8000
# OPTIONAL: pin Supabase auth redirect links (magic-link, password recovery) to a
# fixed host. Leave BLANK to follow the current browser origin (crm.kikichat.de
# today, crm.heykiki.de later) — no rebuild needed on a domain change. Whatever
# host is used must be on Supabase Auth's Redirect-URL allow-list.
VITE_PUBLIC_APP_URL=
